---
apiVersion: v1
kind: ConfigMap
metadata:
  name: argocd-cm
data:
  # to kustomize helm charts
  kustomize.buildOptions: --enable-helm
  resource.exclusions: |
    - apiGroups:
      - cilium.io
      kinds:
      - CiliumIdentity
      clusters:
      - "*"
